```html
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estate Stellare - Parco Villa Filippina</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; overflow-x: hidden; }
        .bg-yellow-main { background-color: #FFEB3B; }
        .bg-blue-dark { background-color: #003B71; }
        .bg-blue-sky { background-color: #81D4FA; }
        .bg-space { background-color: #001F3F; }
        .card-shadow { box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1); }
        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
        .animate-up { animation: slideUp 0.5s ease-out forwards; }
    </style>
</head>
<body class="bg-slate-50">
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // Configurazione Firebase
        const getFirebaseConfig = () => {
            try { if (window.__firebase_config) return JSON.parse(window.__firebase_config); } catch (e) {}
            return { apiKey: "", authDomain: "", projectId: "", storageBucket: "", messagingSenderId: "", appId: "" };
        };

        const config = getFirebaseConfig();
        const appId = window.__app_id || 'campus-villa-filippina-2026';
        const ADMIN_PASS = "campus_filippina_2026";

        if (!firebase.apps.length) firebase.initializeApp(config);
        const auth = firebase.auth();
        const db = firebase.firestore();

        // Icone SVG
        const Icons = {
            Star: () => <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>,
            Check: () => <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"><polyline points="20 6 9 17 4 12"/></svg>,
            Lock: () => <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><rect width="18" height="11" x="3" y="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>,
            User: () => <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        };

        function App() {
            const [view, setView] = useState('home');
            const [user, setUser] = useState(null);
            const [isAdmin, setIsAdmin] = useState(false);
            const [registrations, setRegistrations] = useState([]);
            const [loading, setLoading] = useState(false);
            const [success, setSuccess] = useState(false);

            useEffect(() => {
                auth.onAuthStateChanged(u => {
                    if (!u) auth.signInAnonymously();
                    setUser(u);
                });
            }, []);

            useEffect(() => {
                if (isAdmin && user) {
                    return db.collection('artifacts').doc(appId).collection('registrations')
                        .onSnapshot(snap => setRegistrations(snap.docs.map(doc => ({id: doc.id, ...doc.data()}))));
                }
            }, [isAdmin, user]);

            const handleSignup = async (e) => {
                e.preventDefault();
                setLoading(true);
                const fd = new FormData(e.target);
                const payload = {
                    parent: fd.get('parent'),
                    phone: fd.get('phone'),
                    email: fd.get('email'),
                    child: fd.get('child'),
                    age: fd.get('age'),
                    status: 'In attesa',
                    timestamp: new Date().toISOString()
                };
                try {
                    await db.collection('artifacts').doc(appId).collection('registrations').add(payload);
                    setSuccess(true);
                    setTimeout(() => { setSuccess(false); setView('home'); }, 3000);
                } catch (err) { alert("Errore database."); }
                setLoading(false);
            };

            const Nav = () => (
                <nav className="bg-blue-dark text-white p-4 sticky top-0 z-50 flex justify-between items-center px-6 shadow-xl">
                    <div className="flex items-center gap-2 font-black text-lg cursor-pointer" onClick={() => setView('home')}>
                        <span className="text-yellow-400"><Icons.Star /></span> ESTATE STELLARE
                    </div>
                    <div className="flex gap-3 items-center">
                        <button onClick={() => setView('registration')} className="bg-yellow-main text-blue-dark px-5 py-2 rounded-full font-black text-xs shadow-lg uppercase">Iscriviti Ora</button>
                        <button onClick={() => setView(isAdmin ? 'admin' : 'login')} className="p-2 bg-white/10 rounded-full hover:bg-white/20">
                            <Icons.Lock />
                        </button>
                    </div>
                </nav>
            );

            if (view === 'login') return (
                <div className="min-h-screen bg-slate-100 flex flex-col">
                    <Nav />
                    <div className="flex-1 flex items-center justify-center p-6">
                        <div className="max-w-md w-full bg-white p-10 rounded-[3rem] shadow-2xl border-t-8 border-blue-dark text-center">
                            <h2 className="text-2xl font-black text-blue-dark mb-6">ACCESSO STAFF</h2>
                            <input type="password" id="pass" className="w-full p-4 bg-slate-50 border-2 border-slate-100 rounded-2xl mb-4 text-center font-bold outline-none" placeholder="Password" />
                            <button onClick={() => {
                                if (document.getElementById('pass').value === ADMIN_PASS) { setIsAdmin(true); setView('admin'); }
                                else alert("Accesso negato.");
                            }} className="w-full bg-blue-dark text-white py-4 rounded-2xl font-black uppercase tracking-widest">Entra</button>
                        </div>
                    </div>
                </div>
            );

            if (view === 'admin') return (
                <div className="min-h-screen bg-slate-50">
                    <Nav />
                    <div className="max-w-6xl mx-auto p-6">
                        <div className="flex justify-between items-center mb-8">
                            <h2 className="text-3xl font-black text-blue-dark uppercase tracking-tighter">Iscritti ({registrations.length})</h2>
                            <button onClick={() => setView('home')} className="text-slate-400 font-bold">Torna alla Home</button>
                        </div>
                        <div className="bg-white rounded-[2rem] shadow-xl overflow-hidden border border-slate-200">
                            <div className="overflow-x-auto">
                                <table className="w-full text-left">
                                    <thead className="bg-slate-50 text-[10px] font-black uppercase text-slate-400">
                                        <tr>
                                            <th className="p-5">Bambino</th>
                                            <th className="p-5">Genitore / Contatto</th>
                                            <th className="p-5">Stato</th>
                                            <th className="p-5 text-right">Azioni</th>
                                        </tr>
                                    </thead>
                                    <tbody className="divide-y divide-slate-100 font-bold">
                                        {registrations.map(r => (
                                            <tr key={r.id} className="hover:bg-slate-50 transition">
                                                <td className="p-5">
                                                    <div className="text-blue-dark text-lg">{r.child}</div>
                                                    <div className="text-blue-400 text-xs uppercase">{r.age}</div>
                                                </td>
                                                <td className="p-5">
                                                    <div className="text-slate-700">{r.parent}</div>
                                                    <div className="text-blue-600 text-sm">{r.phone}</div>
                                                    <div className="text-slate-300 text-[10px] font-normal italic lowercase">{r.email}</div>
                                                </td>
                                                <td className="p-5">
                                                    <span className={`px-4 py-1 rounded-full text-[9px] uppercase font-black ${r.status === 'Confermata' ? 'bg-green-100 text-green-700' : 'bg-yellow-100 text-yellow-700'}`}>
                                                        {r.status}
                                                    </span>
                                                </td>
                                                <td className="p-5 text-right">
                                                    <button onClick={() => db.collection('artifacts').doc(appId).collection('registrations').doc(r.id).update({status: 'Confermata'})} className="p-2 text-green-500 bg-green-50 rounded-xl mr-2">OK</button>
                                                    <button onClick={() => {if(confirm("Eliminare?")) db.collection('artifacts').doc(appId).collection('registrations').doc(r.id).delete();}} className="p-2 text-red-400 bg-red-50 rounded-xl">ELIMINA</button>
                                                </td>
                                            </tr>
                                        ))}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            );

            if (view === 'registration') return (
                <div className="min-h-screen bg-slate-100 pb-20">
                    <Nav />
                    <div className="max-w-3xl mx-auto mt-12 p-4">
                        <div className="bg-white rounded-[3rem] shadow-2xl border-[10px] border-blue-sky overflow-hidden animate-up">
                            <div className="bg-blue-sky p-10 text-center">
                                <h2 className="text-4xl font-black text-blue-dark uppercase tracking-tight">MODULO ISCRIZIONE</h2>
                                <p className="text-blue-dark font-bold opacity-70">Completa tutti i campi per prenotare</p>
                            </div>
                            {success ? (
                                <div className="p-20 text-center space-y-6">
                                    <div className="w-20 h-20 bg-green-100 text-green-600 rounded-full flex items-center justify-center mx-auto"><Icons.Check /></div>
                                    <h3 className="text-3xl font-black text-blue-dark">RICHIESTA INVIATA!</h3>
                                    <p className="font-bold text-slate-400 italic leading-tight">Riceverai una conferma su Whatsapp <br/> entro 24 ore.</p>
                                </div>
                            ) : (
                                <form onSubmit={handleSignup} className="p-10 space-y-6">
                                    <div className="grid md:grid-cols-2 gap-6">
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Nome Genitore</label>
                                            <input required name="parent" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-blue-sky font-bold" placeholder="Es. Mario Rossi" />
                                        </div>
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Telefono Whatsapp</label>
                                            <input required name="phone" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-blue-sky font-bold" placeholder="3XX XXXXXXX" />
                                        </div>
                                    </div>
                                    <div className="space-y-1">
                                        <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Email</label>
                                        <input required name="email" type="email" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-blue-sky font-bold" placeholder="esempio@email.it" />
                                    </div>
                                    <div className="grid md:grid-cols-2 gap-6">
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Nome Bambino/a</label>
                                            <input required name="child" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-blue-sky font-bold" placeholder="Nome" />
                                        </div>
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Fascia Età</label>
                                            <select name="age" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none font-bold">
                                                <option>4-5 Anni</option>
                                                <option>6-8 Anni</option>
                                                <option>9-11 Anni</option>
                                            </select>
                                        </div>
                                    </div>
                                    <button disabled={loading} className="w-full bg-blue-dark text-white py-6 rounded-3xl font-black text-2xl shadow-xl hover:scale-[1.02] transition active:scale-95">
                                        {loading ? "INVIO IN CORSO..." : "PRENOTA ORA"}
                                    </button>
                                </form>
                            )}
                        </div>
                    </div>
                </div>
            );

            return (
                <div className="min-h-screen">
                    <Nav />
                    
                    {/* Hero Section */}
                    <header className="bg-yellow-main py-20 px-6 text-center relative overflow-hidden">
                        <div className="max-w-5xl mx-auto relative z-10 animate-up">
                            <h1 className="text-6xl md:text-[120px] font-black text-blue-dark leading-none tracking-tighter uppercase mb-2">ESTATE STELLARE</h1>
                            <h2 className="text-xl md:text-4xl font-black text-blue-dark bg-white/40 inline-block px-8 py-3 rounded-3xl mb-10 tracking-tight">PARCO VILLA FILIPPINA</h2>
                            <div className="flex flex-wrap justify-center gap-4 mb-10">
                                {["CALCIO", "BASKET", "VOLLEY", "PING PONG", "GIOCHI D'ACQUA"].map(tag => (
                                    <span key={tag} className="bg-blue-dark text-white px-5 py-2 rounded-full font-black text-[10px] tracking-widest">{tag}</span>
                                ))}
                            </div>
                            <button onClick={() => setView('registration')} className="bg-blue-dark text-white px-16 py-6 rounded-full font-black text-2xl shadow-2xl hover:scale-105 transition-all">ISCRIVITI ORA!</button>
                        </div>
                        {/* Stelle Decorative */}
                        <div className="absolute top-10 left-10 text-blue-dark/10 rotate-12 scale-150"><Icons.Star /></div>
                        <div className="absolute bottom-10 right-10 text-blue-dark/10 -rotate-12 scale-150"><Icons.Star /></div>
                    </header>

                    {/* Dettagli Periodo */}
                    <section className="bg-blue-sky py-12 px-6 flex flex-col md:flex-row justify-center items-center gap-8 md:gap-20 border-y-4 border-white">
                        <div className="text-center">
                            <div className="text-[10px] font-black uppercase text-blue-dark/50">Periodo</div>
                            <div className="text-2xl md:text-4xl font-black text-blue-dark">15 GIUGNO - 30 LUGLIO</div>
                        </div>
                        <div className="text-center">
                            <div className="text-[10px] font-black uppercase text-blue-dark/50">Orario</div>
                            <div className="text-2xl md:text-4xl font-black text-blue-dark">LUN - VEN | 08:00 - 13:00</div>
                        </div>
                    </section>

                    {/* Programma Settimanale */}
                    <section className="py-20 px-6 bg-white">
                        <div className="max-w-6xl mx-auto">
                            <h3 className="text-4xl md:text-6xl font-black text-blue-dark text-center mb-16 uppercase tracking-tighter italic">PROGRAMMA SETTIMANALE</h3>
                            <div className="grid grid-cols-1 md:grid-cols-5 gap-6">
                                {[
                                    {d: "Lunedì", t: "Sport & Creatività", c: "Calcio, Basket e Laboratorio Maschere."},
                                    {d: "Martedì", t: "Olimpiadi & Stelle", c: "Olimpiadi a squadre e sessione Planetario."},
                                    {d: "Mercoledì", t: "Acqua & Mistero", c: "Giochi d'acqua e Caccia al tesoro spaziale."},
                                    {d: "Giovedì", t: "Arte & Spettacolo", c: "Bolle di sapone e Teatrino dei burattini."},
                                    {d: "Venerdì", t: "Gran Finale", c: "Finali, premiazioni e show dei ragazzi."},
                                ].map((item, i) => (
                                    <div key={i} className="bg-slate-50 p-8 rounded-[2.5rem] border-2 border-slate-100 hover:border-blue-sky transition group">
                                        <div className="text-blue-dark font-black text-2xl mb-2">{item.d}</div>
                                        <div className="text-[10px] font-black text-blue-sky uppercase mb-4 tracking-widest">{item.t}</div>
                                        <p className="text-sm font-bold text-slate-500 leading-tight">{item.c}</p>
                                    </div>
                                ))}
                            </div>
                        </div>
                    </section>

                    {/* Orari Fissi */}
                    <section className="bg-space py-24 px-6 text-white text-center">
                        <div className="max-w-4xl mx-auto space-y-12">
                            <h4 className="text-3xl font-black uppercase tracking-tighter flex items-center justify-center gap-4">
                                <span className="text-yellow-400"><Icons.Star /></span> LA GIORNATA TIPO <span className="text-yellow-400"><Icons.Star /></span>
                            </h4>
                            <div className="space-y-4">
                                {[
                                    {h: "08:00 - 09:00", a: "Accoglienza e Giochi (Ping-Pong / Tavolo)"},
                                    {h: "10:00 - 10:30", a: "Pausa Merenda"},
                                    {h: "12:30 - 13:00", a: "Riordino e Fine Attività"}
                                ].map((slot, i) => (
                                    <div key={i} className="flex flex-col md:flex-row items-center gap-4 bg-white/5 p-6 rounded-3xl border border-white/10 hover:bg-white/10 transition">
                                        <div className="bg-yellow-main text-blue-dark px-6 py-2 rounded-xl font-black text-lg whitespace-nowrap tracking-tighter">{slot.h}</div>
                                        <div className="text-xl font-bold opacity-90">{slot.a}</div>
                                    </div>
                                ))}
                            </div>
                        </div>
                    </section>

                    {/* Contatti Locandina */}
                    <section className="bg-yellow-main py-20 px-6">
                        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-10 items-center">
                            <div className="bg-blue-dark text-white p-10 rounded-[3rem] shadow-2xl">
                                <h4 className="text-2xl font-black mb-6 uppercase">CONTATTI WHATSAPP</h4>
                                <div className="space-y-6">
                                    <a href="tel:3891335731" className="flex items-center gap-4 group">
                                        <div className="bg-green-500 p-3 rounded-full group-hover:scale-110 transition">📞</div>
                                        <div className="text-2xl font-black tracking-tighter italic">389 1335731</div>
                                    </a>
                                    <a href="tel:3420116832" className="flex items-center gap-4 group">
                                        <div className="bg-green-500 p-3 rounded-full group-hover:scale-110 transition">📞</div>
                                        <div className="text-2xl font-black tracking-tighter italic">342 0116832</div>
                                    </a>
                                </div>
                            </div>
                            <div className="text-center md:text-left space-y-4 text-blue-dark">
                                <h4 className="text-3xl font-black uppercase leading-none">Vieni a trovarci a Villa Filippina</h4>
                                <p className="font-bold opacity-70 italic">Un'estate indimenticabile tra sport, stelle e laboratori creativi per ragazzi dai 4 agli 11 anni.</p>
                                <button onClick={() => setView('registration')} className="bg-white px-10 py-4 rounded-full font-black uppercase text-sm shadow-xl hover:bg-blue-dark hover:text-white transition">Modulo Web Rapido</button>
                            </div>
                        </div>
                    </section>

                    <footer className="bg-blue-dark py-12 text-center text-white/20 font-black text-[10px] tracking-[0.4em] uppercase">
                        PARCO VILLA FILIPPINA &bull; URANIA MANAGEMENT &bull; PALERMO 2026
                    </footer>
                </div>
            );
        }

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>

```
