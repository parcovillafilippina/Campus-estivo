```html
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estate Stellare 2026 - Parco Villa Filippina</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- React & ReactDOM -->
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <!-- Babel -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <!-- Firebase -->
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #f8fafc; color: #003B71; }
        .bg-yellow-brand { background-color: #FFEB3B; }
        .bg-blue-brand { background-color: #003B71; }
        .bg-sky-brand { background-color: #81D4FA; }
        .card { transition: all 0.3s ease; border-radius: 2rem; }
        .card:hover { transform: translateY(-5px); box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1); }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // Configurazione Firebase (Sostituisci se hai i tuoi dati, altrimenti usa questo mock)
        const firebaseConfig = {
            apiKey: "", 
            authDomain: "campus-villa-filippina.firebaseapp.com",
            projectId: "campus-villa-filippina",
            storageBucket: "campus-villa-filippina.appspot.com",
            messagingSenderId: "123456789",
            appId: "1:123456789:web:abcdef"
        };

        const appId = "campus-villa-filippina-2026";
        const ADMIN_PASS = "campus_filippina_2026";

        if (!firebase.apps.length) firebase.initializeApp(firebaseConfig);
        const auth = firebase.auth();
        const db = firebase.firestore();

        function App() {
            const [view, setView] = useState('home');
            const [isAdmin, setIsAdmin] = useState(false);
            const [registrations, setRegistrations] = useState([]);
            const [loading, setLoading] = useState(false);
            const [success, setSuccess] = useState(false);

            useEffect(() => {
                auth.signInAnonymously().catch(() => console.log("Offline mode"));
            }, []);

            useEffect(() => {
                if (isAdmin) {
                    const unsubscribe = db.collection('artifacts').doc(appId).collection('registrations')
                        .onSnapshot(snap => {
                            setRegistrations(snap.docs.map(doc => ({id: doc.id, ...doc.data()})));
                        });
                    return () => unsubscribe();
                }
            }, [isAdmin]);

            const handleSignup = async (e) => {
                e.preventDefault();
                setLoading(true);
                const fd = new FormData(e.target);
                try {
                    await db.collection('artifacts').doc(appId).collection('registrations').add({
                        parent: fd.get('parent'),
                        phone: fd.get('phone'),
                        child: fd.get('child'),
                        age: fd.get('age'),
                        date: new Date().toLocaleString()
                    });
                    setSuccess(true);
                    setTimeout(() => { setSuccess(false); setView('home'); }, 3000);
                } catch (err) {
                    alert("Errore nell'invio. Riprova più tardi.");
                }
                setLoading(false);
            };

            const Nav = () => (
                <nav className="bg-blue-brand text-white p-4 sticky top-0 z-50 flex justify-between items-center px-6 shadow-xl border-b-4 border-yellow-brand">
                    <div className="font-black text-xl flex items-center gap-2 cursor-pointer italic" onClick={() => setView('home')}>
                        ⭐ ESTATE STELLARE
                    </div>
                    <div className="flex gap-4 items-center">
                        <button onClick={() => setView('registration')} className="bg-yellow-brand text-blue-brand px-5 py-1.5 rounded-full font-black text-xs uppercase tracking-tighter">Iscriviti</button>
                        {/* TASTO ADMIN (INGRANAGGIO) */}
                        <button onClick={() => setView(isAdmin ? 'admin' : 'login')} className="text-white/40 hover:text-white transition-colors text-xl">
                            ⚙️
                        </button>
                    </div>
                </nav>
            );

            if (view === 'login') return (
                <div className="min-h-screen"><Nav />
                    <div className="max-w-md mx-auto mt-20 p-10 bg-white rounded-[2.5rem] shadow-2xl text-center border-t-8 border-blue-brand">
                        <h2 className="text-2xl font-black mb-6 uppercase tracking-tighter text-blue-brand italic">Accesso Gestione</h2>
                        <input type="password" id="pw" className="w-full p-4 bg-slate-50 rounded-xl mb-4 text-center font-bold border-2 border-slate-100 outline-none focus:border-yellow-brand" placeholder="Inserisci Password" />
                        <button onClick={() => { if(document.getElementById('pw').value === ADMIN_PASS) {setIsAdmin(true); setView('admin');} else alert("Password Errata!"); }} className="w-full bg-blue-brand text-white py-4 rounded-xl font-black uppercase tracking-widest">Entra</button>
                    </div>
                </div>
            );

            if (view === 'admin') return (
                <div className="min-h-screen bg-slate-50"><Nav />
                    <div className="max-w-6xl mx-auto p-6">
                        <div className="flex justify-between items-center mb-8">
                            <h2 className="text-3xl font-black italic uppercase text-blue-brand underline decoration-yellow-brand underline-offset-8">Richieste Iscrizione ({registrations.length})</h2>
                            <button onClick={() => setView('home')} className="text-slate-400 font-bold">Torna al sito</button>
                        </div>
                        <div className="bg-white rounded-[2rem] shadow-xl overflow-hidden border border-slate-100">
                            <table className="w-full text-left">
                                <thead className="bg-slate-50 text-[10px] font-black uppercase text-slate-400 border-b">
                                    <tr><th className="p-5">Bambino/a</th><th className="p-5">Genitore</th><th className="p-5">Telefono</th><th className="p-5">Data Invio</th><th className="p-5">Azioni</th></tr>
                                </thead>
                                <tbody className="font-bold divide-y divide-slate-50">
                                    {registrations.length === 0 ? <tr><td colSpan="5" className="p-10 text-center text-slate-300">Nessuna iscrizione ricevuta</td></tr> : null}
                                    {registrations.map(r => (
                                        <tr key={r.id} className="hover:bg-slate-50/50">
                                            <td className="p-5"><div className="text-blue-900">{r.child}</div><div className="text-[10px] opacity-40 uppercase">{r.age}</div></td>
                                            <td className="p-5">{r.parent}</td>
                                            <td className="p-5 text-blue-500">{r.phone}</td>
                                            <td className="p-5 text-[10px] opacity-30 italic">{r.date}</td>
                                            <td className="p-5"><button onClick={() => {if(confirm("Eliminare?")) db.collection('artifacts').doc(appId).collection('registrations').doc(r.id).delete()}} className="text-red-300 hover:text-red-500 transition-colors">Elimina</button></td>
                                        </tr>
                                    ))}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            );

            if (view === 'registration') return (
                <div className="min-h-screen bg-slate-50 pb-20"><Nav />
                    <div className="max-w-3xl mx-auto mt-10 p-4 animate-fade-up">
                        <div className="bg-white rounded-[3rem] shadow-2xl border-[12px] border-sky-brand overflow-hidden">
                            <div className="bg-sky-brand p-10 text-center text-blue-900">
                                <h2 className="text-4xl font-black uppercase tracking-tighter italic">Richiesta Iscrizione</h2>
                                <p className="font-bold opacity-60">Estate Stellare 2026 - Villa Filippina</p>
                            </div>
                            {success ? (
                                <div className="p-20 text-center space-y-6">
                                    <div className="text-6xl">✨</div>
                                    <h3 className="text-3xl font-black text-blue-brand uppercase">Ricevuto!</h3>
                                    <p className="font-bold text-slate-400">Verrai contattato dallo staff entro 24 ore.</p>
                                </div>
                            ) : (
                                <form onSubmit={handleSignup} className="p-10 space-y-6">
                                    <div className="grid md:grid-cols-2 gap-6">
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Nome Genitore</label>
                                            <input required name="parent" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-yellow-brand font-bold" />
                                        </div>
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Cellulare</label>
                                            <input required name="phone" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-yellow-brand font-bold" />
                                        </div>
                                    </div>
                                    <div className="grid md:grid-cols-2 gap-6">
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Nome Bambino/a</label>
                                            <input required name="child" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none focus:border-yellow-brand font-bold" />
                                        </div>
                                        <div className="space-y-1">
                                            <label className="text-[10px] font-black uppercase text-slate-400 ml-4">Fascia d'età</label>
                                            <select name="age" className="w-full bg-slate-50 border-2 border-slate-100 p-5 rounded-2xl outline-none font-bold">
                                                <option>4-5 Anni</option>
                                                <option>6-8 Anni</option>
                                                <option>9-11 Anni</option>
                                            </select>
                                        </div>
                                    </div>
                                    <button disabled={loading} className="w-full bg-blue-brand text-white py-6 rounded-3xl font-black text-2xl shadow-xl hover:scale-105 transition uppercase tracking-tighter">Prenota il posto</button>
                                </form>
                            )}
                        </div>
                    </div>
                </div>
            );

            return (
                <div className="animate-fade">
                    <Nav />
                    {/* Hero Section */}
                    <header className="bg-yellow-brand py-20 px-6 text-center border-b-8 border-blue-brand relative overflow-hidden">
                        <div className="max-w-5xl mx-auto relative z-10">
                            <h1 className="text-6xl md:text-[110px] font-black text-blue-brand leading-none tracking-tighter uppercase mb-6 italic">ESTATE STELLARE</h1>
                            <h2 className="text-xl md:text-3xl font-black text-blue-brand bg-white/40 inline-block px-10 py-3 rounded-2xl mb-12">PARCO VILLA FILIPPINA</h2>
                            <div className="flex flex-wrap justify-center gap-4 mb-12">
                                {["CALCIO", "BASKET", "SCIENZA", "BOZZE GIGANTI", "TEATRINO"].map(t => (
                                    <span key={t} className="bg-blue-brand text-white px-5 py-2 rounded-full font-black text-[10px] tracking-widest">{t}</span>
                                ))}
                            </div>
                            <button onClick={() => setView('registration')} className="bg-blue-brand text-white px-16 py-6 rounded-full font-black text-3xl shadow-2xl hover:scale-105 transition uppercase tracking-tighter">Iscriviti Ora</button>
                        </div>
                        <div className="absolute top-10 right-10 text-blue-brand/5 text-9xl">⭐</div>
                    </header>

                    {/* Programma */}
                    <section className="py-20 px-6 max-w-7xl mx-auto">
                        <h3 className="text-4xl md:text-6xl font-black text-center mb-16 uppercase tracking-tighter italic decoration-yellow-brand underline underline-offset-8">Cronoprogramma Settimanale</h3>
                        <div className="grid grid-cols-1 md:grid-cols-5 gap-6">
                            {[
                                {d: "Lunedì", t: "Sport & Creatività", c: "Percorsi motori palla (Calcio/Basket) + Laboratorio Creativo Maschere planetarie."},
                                {d: "Martedì", t: "Olimpiadi & Stelle", c: "Staffette, lancio vortex. Attività Scientifica: Sessione Planetario e Telescopio."},
                                {d: "Mercoledì", t: "Acqua & Mistero", c: "Giochi d'acqua (gavettoni e scivoli) + Caccia al tesoro a tema spaziale."},
                                {d: "Giovedì", t: "Show & Burattini", c: "Ping-pong e Speciale: Spettacolo di bolle di sapone giganti e Teatrino."},
                                {d: "Venerdì", t: "Gran Finale", c: "Finali tornei, premiazioni e show finale dei ragazzi."}
                            ].map((item, i) => (
                                <div key={i} className={`bg-white p-8 card border-4 ${i === 3 ? 'border-yellow-brand shadow-2xl scale-105' : 'border-slate-100'}`}>
                                    <div className="text-blue-brand font-black text-2xl mb-2 italic">{item.d}</div>
                                    <div className="text-[10px] font-black text-sky-500 uppercase mb-4 tracking-widest">{item.t}</div>
                                    <p className="text-sm font-bold text-slate-500 leading-tight">{item.c}</p>
                                </div>
                            ))}
                        </div>
                    </section>

                    {/* Orari Fissi */}
                    <section className="bg-blue-brand py-24 px-6 text-white text-center">
                        <div className="max-w-4xl mx-auto space-y-12">
                            <h4 className="text-3xl font-black uppercase italic underline decoration-yellow-brand underline-offset-8">La Giornata Tipo</h4>
                            <div className="space-y-6">
                                {[
                                    {h: "08:00 - 09:00", a: "Accoglienza e giochi liberi (Ping-pong / Giochi da tavolo)"},
                                    {h: "10:00 - 10:30", a: "Pausa Merenda Rigenerante"},
                                    {h: "12:30 - 13:00", a: "Riordino e Uscita guidata"}
                                ].map((slot, i) => (
                                    <div key={i} className="flex flex-col md:flex-row items-center gap-6 bg-white/10 p-8 rounded-[2rem] border border-white/10 hover:bg-white/15 transition shadow-2xl">
                                        <div className="bg-yellow-brand text-blue-brand px-8 py-3 rounded-2xl font-black text-2xl shadow-lg min-w-[200px]">{slot.h}</div>
                                        <div className="text-2xl font-bold opacity-90 italic">{slot.a}</div>
                                    </div>
                                ))}
                            </div>
                        </div>
                    </section>

                    {/* Footer / Contatti */}
                    <footer className="bg-yellow-brand py-20 px-6 border-t-8 border-blue-brand">
                        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-12 items-center">
                            <div className="bg-blue-brand text-white p-10 rounded-[3rem] shadow-2xl">
                                <h5 className="font-black mb-8 uppercase tracking-widest flex items-center gap-2 italic">⭐ Contatti Rapidi</h5>
                                <div className="space-y-6">
                                    <div className="bg-white/10 p-6 rounded-2xl flex items-center justify-between">
                                        <span className="font-black text-3xl">389 1335731</span>
                                        <span className="text-green-400">💬</span>
                                    </div>
                                    <div className="bg-white/10 p-6 rounded-2xl flex items-center justify-between">
                                        <span className="font-black text-3xl">342 0116832</span>
                                        <span className="text-green-400">💬</span>
                                    </div>
                                </div>
                            </div>
                            <div className="text-blue-brand space-y-6 text-center md:text-left">
                                <h5 className="text-4xl font-black uppercase leading-none italic">Un'estate tra sport e stelle!</h5>
                                <p className="text-xl font-bold opacity-70 italic leading-snug">Vieni a trovarci al Parco Villa Filippina per un'esperienza indimenticabile e sicura per i tuoi figli.</p>
                                <div className="pt-6 text-[10px] font-black uppercase tracking-[0.4em] opacity-30">Urania Management &bull; Palermo 2026</div>
                            </div>
                        </div>
                    </footer>
                </div>
            );
        }

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>

```
